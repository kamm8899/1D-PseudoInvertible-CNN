# Superseded checkpoint notice

This completed sweep is retained for provenance, but it must not be cited in the
resubmission. Its `config.json` records CAE checkpoint
`compare_A_lowcap-original.pth`. On 26 September 2026, the advisor confirmed
`cae_best.pth` as the paper CAE checkpoint. Run `evaluate_nonlinearity.py` again
with a new output directory after completing the corrected-data paper runs.
